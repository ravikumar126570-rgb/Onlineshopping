function AboutWebsitePage() {
  return (
    <section className="card info-card">
      <span className="eyebrow">About Website</span>
      <h1>React + Vite storefront with practical ecommerce features</h1>
      <p>
        MicroFlip Shop uses React Router for navigation, Context API for cart state, JSON for base
        product storage, LocalStorage for persistence, and responsive CSS for a modern shopping
        interface.
      </p>
      <p>
        The experience includes category filters, search, dark mode, admin product creation,
        product details, checkout UI, and a custom 404 page, making it a strong starter project for
        ecommerce portfolios.
      </p>
    </section>
  );
}

export default AboutWebsitePage;
